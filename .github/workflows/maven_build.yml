# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17 for x64
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        architecture: x64
        cache: maven
    - name: Build with Maven
      run: mvn --batch-mode --update-snapshots verify
    - name: Create directory for staging to be included later as a gh release binary package
      run: mkdir staging && cp target/*.jar staging
    - name: Upload the packaged binaries to Github
      uses: actions/upload-artifact@v4
      with:
        name: openai-connector
        path: staging

  docker:

    runs-on: ubuntu-latest

    steps:
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with images: name/app

      - name: Login to Docker registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          # Server address of docker registry
#          registry: No need for dockerhub
          username: soulou2019
          # Password or personal registry api access token
          password: dckr_pat_SjgEiOU8LRuPLQ4dtWKxL9jtQ4o

      - name: Build and push Docker image
        if: github.event_name != 'pull_request'
        run: docker compose build && docker compose push
#      - run:
#      - name: Upload the path to the Docker image
#        uses: actions/upload-artifact@v4
#        with:
#          name: openai-connector-docker
#          path: staging
